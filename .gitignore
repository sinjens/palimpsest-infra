# User's live config (they should copy config.example.toml to ~/.claude/palimpsest/
# — this guard is for cases where someone edits the example in place).
config/config.toml

# OS / editor detritus
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# Python cache (in case we ever add tooling locally)
__pycache__/
*.pyc
.venv/
.pytest_cache/
